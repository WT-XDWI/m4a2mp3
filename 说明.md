# 音频 → MP3 批量转换

纯浏览器本地转换，文件不上传。

## 启动方式

**双击 `启动.bat`**（需要 Node.js 或 Python，二选一即可）。

浏览器会自动打开 `http://127.0.0.1:8420/index.html`。

> **为什么不能直接双击 index.html？**
> 浏览器禁止 `file://` 页面创建 Worker，而 ffmpeg.wasm 必须依赖 Worker。
> 所以直接双击打开一定会报 `cannot be accessed from origin 'null'`。
> 必须通过上面的本地服务打开，这是浏览器的硬性限制，无法绕过。

## 支持的输入格式

| 类型 | 格式 |
|---|---|
| Apple / AAC | m4a、m4b、m4r、aac、mp4、m4v、mov、3gp、caf |
| 有损 | mp3、ogg、opus、wma、amr、ac3、dts、mp2、ra |
| 无损 / 未压缩 | wav、flac、aiff、ape、wv、tta、alac、dsf、au |
| 视频（自动取音轨） | mkv、webm、avi、flv、wmv、mpg、ts、m2ts、vob |

## 功能

- 拖拽或点击，一次添加多个文件
- 码率 96–320 kbps
- 采样率、声道（保持 / 立体声 / 单声道）
- 单文件下载 + 一键打包 ZIP
- 可中途停止，未完成的可以继续

## 注意事项

- **MP3 输入会原样复制**，不重新编码（避免二次损失音质）。若你手动改了采样率或声道，则会重新编码。
- 选 **≤96 kbps 且源是 44.1 kHz** 时，编码器会自动降到 32 kHz（MPEG-1 规范限制），页面会提示。
- 转换在内存中进行，**单个文件越大占用内存越多**。几百 MB 的文件建议用命令行版本。
- 首次启动需加载约 31 MB 的 wasm 引擎（本地读取，不走网络）。
